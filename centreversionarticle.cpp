#include "centreversionarticle.h"
#include <QMessageBox>

/*
CentreVersionArticle::CentreVersionArticle (Article& art, QWidget *parent):
    QWidget(parent), CentreVersion(art, parent), article(art)
{

    texte    = new QTextEdit(this);

    textel   = new QLabel("Titre", this);

    ctexte = new QHBoxLayout;
    ctexte->addWidget(textel);
    ctexte->addWidget(texte);

    couche->addLayout(ctexte);
    couche->addLayout(cboutons);

    texte->setText(art->getText());

    setLayout(couche);

    QObject::connect(texte, SIGNAL(textChanged()),this, SLOT(activerSauvegarde()));

}

void CentreVersion::sauvegarderArticle() {

    article->setText(texte->toPlainText());
    article->sauvegarderVersion();

    sauvegarder->setEnabled(false);
}
*/
